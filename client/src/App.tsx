// Obsidian Command Deck: one focused route keeps the Telegram gateway and player command center in a single reliable surface.
import Home from '@/pages/Home';

export default function App() {
  return <Home />;
}
