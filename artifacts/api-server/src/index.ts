import app from "./app";

/**
 * Serverless entrypoint. Vercel imports this default export as the request handler.
 * The HTTP listener lives in server.ts for local and container execution.
 */
export default app;
