# Telegram bot username without the @ symbol.
VITE_TELEGRAM_BOT_USERNAME=jjk_rpg_bot

# Use the same-domain default (/api) when the API is proxied by your host.
# For a separate API deployment, set the complete API origin ending in /api.
VITE_API_BASE_URL=
